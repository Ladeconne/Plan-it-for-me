const counter = document.querySelector("#counter");
const activityCheckBoxes = document.querySelectorAll("input[type=checkbox]");

const activityCounter = () => {
  activityCheckBoxes.addEventListener("click", () => {
    
  } 
};

export { activityCounter };
