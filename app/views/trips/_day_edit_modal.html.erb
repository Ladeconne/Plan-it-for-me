<i type="button" class="fas fa-plus" data-toggle="modal" data-target="#moal-<%= day.id %>">
</i>

<!-- Modal -->
<div class="modal fade" id="moal-<%= day.id %>" tabindex="-1" role="dialog" aria-labelledby="moal-<%= day.id %>Label" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h2 class="modal-title font-weight-bold" id="moal-<%= day.id %>Label">Add activity</h2>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <% day.trip.activities.where(day: nil).each do |activity| %>
          <div class="title d-flex justify-content-between">
            <div class="">
              <h4><%= activity.name %></h4>
            </div>
            <div>
              <%= link_to assign_day_activity_path(activity, day_id: day.id), method: :patch do %>
                <i class="fas fa-plus"></i>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
